BACKEND
    matcher
    queries
        show all artists/albums that have a missing album 
    scanner
        case insensitive search for mp3
        remove albums / artists if the directory has been removed :(
UI
    scan page - display scan information

DONE
add spotify id to artist
add logic to not lookup spotifyid if it exists
add albums lookups for artist ID
write matched albums back to db
run matches against all artists
write an update script to ignore artists
show all artists/albums that have a release in year ?
UI add "list non flac albums"
UI add scan button
scanner remove wanted if we have a flac
scanner update folders if they have changed
wire wanted check box to artist